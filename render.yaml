services:
  - name: papi-backend
    type: web
    runtime: python
    plan: free
    region: ohio

    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
